# Flask App Configuration
FLASK_DEBUG=True
PORT=5001
SECRET_KEY=enterprise-super-secret-key-3dd0d6d8

# Database Path
DATABASE_PATH=instance/app.db

# SMTP Asynchronous Email Configuration (Local Debugger Example)
# Run in separate terminal: python -m aiosmtpd -n -l localhost:1025
SMTP_SERVER=localhost
SMTP_PORT=1025
SMTP_USE_TLS=False
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_DEFAULT_SENDER="TaskCorp Alerts <notifications@enterprise.internal>"

# Production Gmail Example (uncomment to use):
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_TLS=True
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# MAIL_DEFAULT_SENDER="TaskCorp Alerts <your-email@gmail.com>"
