[project]
name = "employee-management"
version = "1.0.0"
description = "Employee Management and Task Allocation Web Application"
requires-python = ">=3.10"
dependencies = [
    "Flask==3.0.3",
    "Werkzeug==3.0.3",
    "python-dotenv==1.0.1"
]

[tool.vercel]
entrypoint = "api/index.py"
